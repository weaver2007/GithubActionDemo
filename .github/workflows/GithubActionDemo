# 在All Workflows菜单下显示的名称
name: GitHub Actions Demo
# 在workflow runs 菜单下显示的名称 github.actor 会被替换成触发构建的人员ID
run-name: ${{ github.actor }} is testing out GitHub Actions ??
# 触发运行的时机 - [push]只要有代码推送就会触发
on: [push]
# 该工作流中要执行的具体动作
jobs:
  # 动作名称
  Explore-GitHub-Actions:
    # 运行环境
    runs-on: ubuntu-latest
    # 动作步骤
    steps:
      # github.event_name 会被换成 push
      - run: echo "?? The job was automatically triggered by a ${{ github.event_name }} event."
      # runner.os 会被换成 Linux server hosted by GitHub
      - run: echo "?? This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      # github.ref 会被换成 refs/heads/weaver2007-patch-1
      - run: echo "?? The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      # actionscheckout@v3 应该是一个预定义的动作
      # 把 GithubActionDemo checkout 到了 /home/runner/work/GithubActionDemo/GithubActionDemo 目录
      - name: Check out repository code
        uses: actions/checkout@v3
      # github.repository 替换为 weaver2007/GithubActionDemo
      - run: echo "?? The ${{ github.repository }} repository has been cloned to the runner."
      # 显示是一个eco 提示
      - run: echo "??? The workflow is now ready to test your code on the runner."
      # 列出工作目录下的文件
      - name: List files in the repository
        # github.workspace 替换成 http/runner/work/GithubActionDemo/GithubActionDemo
        run: |
          ls ${{ github.workspace }}
        # 显示此job的构建结果 job.status 替换为 success
      - run: echo "?? This job's status is ${{ job.status }}."
